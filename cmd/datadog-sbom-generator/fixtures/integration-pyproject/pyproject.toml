[project]
name = "my-service"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "requests==2.28.0",
    "flask==2.3.2",
    "numpy>=1.24",
]

[project.optional-dependencies]
dev = [
    "pytest==8.2.0",
]

[tool.uv]
